(function(){
    // Central app config (change ENV to 'production' when you deploy)
    window.AppConfig = window.AppConfig || {
        APP_VERSION: '0.2.0',
        ENV: 'staging' // change to 'production' in live deployments
    };
})();